# COL-11: Validate final CERT schema compliance

**Test Type:** Unit  
**Status:** PASS  


---

## Prerequisites

- Python 3.10+ installed
- `pandas` installed 
- Run from the `dist/local` directory

---

## Steps to Reproduce

1. Open a PowerShell or Terminal window and navigate to the `dist/local` directory.
2. Open a Python shell:
   ```
   python
   ```
3. Import pandas and the target processor file:
   ```python
   import pandas as pd
   from local_preprocessor import SCRIPT_DIR
   ```
4. Read the internal code of the preprocessor to inspect the final schema definition explicitly. Alternatively, if a `local_model_intake.csv` has already been generated by running the pipeline, read its headers directly:
   ```python
   # Check the actual exported file (if you have run the pipeline)
   intake_path = SCRIPT_DIR / 'local_model_intake.csv'
   
   if intake_path.exists():
       df = pd.read_csv(intake_path, nrows=0) # Only load headers
       headers = list(df.columns)
       print(f"Total columns: {len(headers)}")
       print(f"Columns: {headers}")
   else:
       print("local_model_intake.csv not found. Please run local_preprocessor.py first.")
   ```
5. Compare the output to the strictly required 16 columns.

---

## Expected Output

The final merge and export function forces the output dataframe to match exactly 16 predefined columns in a specific order before saving to disk.

```
Total columns: 16
Columns: ['date', 'user', 'day', 'total_active_minutes_day', 'after_hours_session_count', 'weekend_session_flag', 'logon_count_zscore', 'logon_count_zscore_has_baseline', 'usb_count', 'usb_after_hours_flag', 'usb_on_weekend_flag', 'usb_device_diversity_monthly', 'usb_count_zscore', 'usb_count_zscore_has_baseline', 'job_site_visits_flag', 'job_search_plus_usb_week']
```

**📸 Screenshot:** Take a screenshot of the terminal output showing exactly 16 columns matching the above schema array. This proves Phase 1 perfectly maps the raw local data into the precise mathematical shape required by the Phase 3 ML models.

---

## Actual Output


```python

>>> import pandas as pd
>>> from local_preprocessor import SCRIPT_DIR
>>> # Check the actual exported file (if you have run the pipeline)
>>> intake_path = SCRIPT_DIR / 'local_model_intake.csv'
>>> 
>>> if intake_path.exists():
...     df = pd.read_csv(intake_path, nrows=0) # Only load headers
...     headers = list(df.columns)
...     print(f"Total columns: {len(headers)}")
...     print(f"Columns: {headers}")
... else:
...     print("local_model_intake.csv not found. Please run local_preprocessor.py first.")
... 
Total columns: 16
Columns: ['date', 'user', 'day', 'total_active_minutes_day', 'after_hours_session_count', 'weekend_session_flag', 'logon_count_zscore', 'logon_count_zscore_has_baseline', 'usb_count', 'usb_after_hours_flag', 'usb_on_weekend_flag', 'usb_device_diversity_monthly', 'usb_count_zscore', 'usb_count_zscore_has_baseline', 'job_site_visits_flag', 'job_search_plus_usb_week']

```

